/'* \file act_minicmd.bas
\brief Signal handler for a GtkAction (id="action461")

\since 3.0
'/

SUB act_minicmd CDECL ALIAS "act_minicmd" ( _
  BYVAL action AS GtkAction PTR, _
  BYVAL user_data AS gpointer) EXPORT

?*__("callback act_minicmd")

END SUB
