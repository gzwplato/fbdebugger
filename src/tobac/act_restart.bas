/'* \file act_restart.bas
\brief Signal handler for a GtkAction (id="action462")

\since 3.0
'/

SUB act_restart CDECL ALIAS "act_restart" ( _
  BYVAL action AS GtkAction PTR, _
  BYVAL user_data AS gpointer) EXPORT

?*__("callback act_restart")

END SUB
