/'* \file act_fastrun.bas
\brief Signal handler for a GtkAction (id="action008")

\since 3.0
'/

SUB act_fastrun CDECL ALIAS "act_fastrun" ( _
  BYVAL action AS GtkAction PTR, _
  BYVAL user_data AS gpointer) EXPORT

?*__("callback act_fastrun")

END SUB
